import { expect } from "chai";
import router from "../src/api";

describe("API Tests", () => {
  describe("/bid", () => {
    it("works", () => {
      console.log("success");
      expect(1 == 1);
    });
  });
});
